#!/bin/sh
# Periodic state save: the whole system lives in RAM; this snapshots changed
# config/state (per the lbu include list) to the boot media every 15 minutes.
# All safety checks (provisioned marker, free space, fit) live in pipeos-save.
exec /usr/local/bin/pipeos-save
