# pipeOS: commit overlay to the boot media (mounted by label at /media/usb —
# see /etc/fstab; works for NVMe, USB stick, or SATA alike)
LBU_MEDIA=usb
# keep a couple of previous apkovl generations on the partition
BACKUP_LIMIT=3
