pool pool.ntp.org iburst
driftfile /var/lib/chrony/chrony.drift
rtcsync
# step the clock on any offset at any time — diskless boxes can come up with a
# wildly wrong clock, and TLS/API auth needs correct time immediately
makestep 1.0 -1
