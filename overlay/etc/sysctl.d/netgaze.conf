# netgaze (pipeOS#217): its ICMP sweep uses SOCK_DGRAM/IPPROTO_ICMP sockets,
# which the kernel grants to the gids in this range — every uid here, so the
# collector runs as the dashboard user, never root.
net.ipv4.ping_group_range = 0 2147483647
