# pipeOS doas policy: wheel members may escalate with THEIR OWN password
# (which is why the dashboard requires a password before granting sudo — a
# key-only '*' account has nothing for doas to check). persist keeps the
# grace window so a run of commands asks once. Deliberately NOT nopass: a
# stolen browser session must not be a free root shell.
permit persist :wheel
